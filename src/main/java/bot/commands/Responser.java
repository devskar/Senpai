package bot.commands;

import bot.Privat;


import net.dv8tion.jda.core.events.message.guild.GuildMessageReceivedEvent;

/**
 * Coded by Oskar#7402
 * At 30.05.2018
 * github.com/oskardevkappa/
 */

public class Responser {

    public static void action(GuildMessageReceivedEvent event) {

/*
        String str = event.getMessage().getContentRaw().replaceFirst("(?i)" + Privat.realPrefix, "");

        try {
            CleverbotAPI api = null;

            if (api == null)
                api = create();


            System.out.println(api.askQuestion(str));

        }catch (Exception e){
            e.printStackTrace();
        }



    }

    private static CleverbotAPI create(){

        CleverbotAPI api = new CleverbotBuilder()
                .setKeys(Privat.CLEVERBOTIOUSERKEY, Privat.CLEVERBOTIOAPIKEY)
                .setNickname("DiscordBot")
                .build();
        return api;*/
    }

}
